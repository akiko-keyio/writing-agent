---

description: Point to spec

alwaysApply: true

---



项目旨在构建 Writing Agent



`docs/writing-agent-design.md` 描述了所有设计的基础



UI 使用 [coss ui](https://coss.com/ui) 组件库，通过 shadcn CLI 从 `@coss/*` registry 安装；配置见 `frontend/components.json`。已安装 Agent Skill：`npx skills add cosscom/coss`（见 [Skills 文档](https://coss.com/ui/docs/skills)）。

**Chat / AI 表现层** 使用 [Nexus UI](https://nexus-ui.dev/docs) 散件（`@nexus-ui/*`，拷贝源码后适配 coss）。**不要**引入 `assistant-ui` 会话 runtime。

如果组件库存在现成的组件或规范，一定不要自己重新造。

## Nexus UI 约定（Chat / AI 散件）

- **先抄 registry，再小改**：Chat 相关用 `cd frontend && pnpm dlx shadcn@latest add @nexus-ui/<name> -y -o`，源码放进 `src/components/nexus-ui/`。**不要**用 coss 的 `Field` 版 `Textarea` 去「仿」Nexus 输入框——会高度乱跳。若 registry 自带扁平 `textarea`，放在 `nexus-ui/` 里专供 PromptInput；**不要**用 CLI 覆盖全局 `src/components/ui/textarea.tsx`（表单仍用 coss）。
- **安装后必做**：① 删掉误生成的 `frontend/components/` 副本；② 若 `ui/tooltip` 等被覆盖，从 git 恢复 coss 版；③ 只改 Nexus 文件里的 import（`@/components/ui/button` 等）、Tooltip 用 coss 的 `TooltipPopup`、图标统一为 `@hugeicons/react` + `@hugeicons/core-free-icons`（`<HugeiconsIcon icon={…Icon} />`）。
- **组装照官方**：`PromptInput` → `ScrollArea`（`max-h-40`）→ `Textarea` → `PromptInputActions`；外层**不要**再包 `border-t` 大框，只留 `PromptInput` 圆角卡片 + 必要 `px` 留白。
- **分工**：coss = 壳层与通用控件；Nexus = Chat 消息/输入散件；会话状态用 `agent-client` / `use-agent-session`，**不要** `assistant-ui` runtime。
- **Review**：coss 自研（`writing-agent-design.md` §5）。

## COSS UI 约定（必须遵循）

- **组件**：优先 `@/components/ui/*` primitive 与 [Particles](https://coss.com/ui/particles)；trigger 类组件用 `render` prop，不用 Radix `asChild` 思维。
- **组件 API 完整性**：`ui/*` 下的 coss 组件只能使用上游文档（`.agents/skills/coss/references/primitives/<name>.md`）中明确列出的 variant / size / prop。禁止自行添加、扩展、删除任何 variant 或 size——即使看起来"合理推测"也不行。如需新值，须在 PR 中注明偏离上游的原因。
- **图标**：全项目统一 Hugeicons（`components.json` → `iconLibrary: "hugeicons"`）。渲染入口 `@/lib/icons` 的 `HugeiconsIcon`；图标数据从 `@hugeicons/core-free-icons` 导入。用法：`<HugeiconsIcon icon={XxxIcon} aria-hidden="true" />`；动态图标用 `IconSvgElement`。禁止 `lucide-react` 及其它图标库。装饰性图标加 `aria-hidden="true"`；仅图标按钮用 `aria-label`。
- **样式**：用语义 token（`bg-background`、`text-muted-foreground` 等），禁止硬编码 palette；布局用 `flex flex-col gap-*`，不用 `space-y-*` / `space-x-*`；优先 `in-[[data-slot=…]]` 而非 `group`。
- **间距**：应用层（壳层 / Chat / Nexus 适配 / 业务组件）统一从 `@/lib/spacing` 取 `gap` / `p` / `stack` / `row`；禁止裸写 `gap-*` / `p-*` / `space-y-*`。`ui/*` primitive 内置间距不改。
- **主题**：CSS 变量链见 `frontend/src/index.css`（`:root` 定义 `--font-sans` / `--font-heading` / `--font-mono`，`@theme inline` 映射）；保留 `--alpha()` token，勿改为 `color-mix` 替代 border。
- **全局布局**：`body.relative` + 根容器 `isolate relative flex min-h-svh flex-col`；`ToastProvider` / `TooltipProvider` 包裹应用。
- **CLI 后处理**：`npx shadcn add @coss/<name>` 若生成 lucide import，须改为 `@hugeicons/core-free-icons` + `@/lib/icons` 的 `HugeiconsIcon`；保留 `aria-hidden`。勿运行 `@coss/style` 覆盖已有主题，除非有意全量同步。
- **应用壳层**：布局 token 见 `shell-chrome.ts`（间距从 `spacing.ts` 导入）；**图标钮只用** coss `Button`（`ShellIconButton` = ghost `icon-sm`），侧栏行用 `SidebarMenuButton size="sm"`，**禁止**自造 `<button>` 或手写 `[&_svg]:size-*` 覆盖 coss 图标尺寸。Open Folder 在 File 菜单；打开 `.md` 仅在 Files 树。见 `docs/frontend-ui-handoff.md`。

