{
  "id": "check-then-edit",
  "suites": ["smoke", "multiagent"],
  "description": "Main agent runs the deterministic check_consistency tool, then proposes a validated edit group based on the finding. No direct document mutation.",
  "autopilot_policy": "suggest_only",
  "document_path": "methods.md",
  "document": "# Methods\n\nThe colour model is fixed. We use a consistent color throughout.\n",
  "instruction": "Check the methods section for consistency and fix any issue.",
  "model_script": [
    { "tools": [{ "name": "check_consistency", "input": { "path": "methods.md" } }] },
    {
      "tools": [
        {
          "name": "propose_edit_group",
          "input": {
            "path": "methods.md",
            "title": "Consistent spelling",
            "summary": "Normalize 'colour' to 'color'.",
            "edits": [
              { "kind": "replace", "old_text": "colour", "new_text": "color" }
            ]
          }
        }
      ]
    },
    { "text": "Proposed a fix based on the consistency check." }
  ],
  "expect": {
    "must_call": ["check_consistency", "propose_edit_group"],
    "must_create_group": true,
    "valid_anchor_count": "all",
    "must_not_emit": ["document/patch"],
    "document_unchanged": true
  }
}
