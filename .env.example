# LLM (OpenAI-compatible API)
OPENAI_API_BASE=https://your-provider.example/v1
OPENAI_API_KEY=your-api-key
OPENAI_MODEL=your-model

# Agent WebSocket server
WS_HOST=127.0.0.1
WS_PORT=8765

# Default document (relative to project root)
DEFAULT_DOCUMENT=examples/test-text.md

# Edit defaults (settings panel deferred)
DEFAULT_EDIT_CHOICE=new
